---
super-roast verdict: Blocking (5 confirmed) [low coverage] [panel-capped: 8 unverified]
mode: PR        iteration: 1 of 3
profile (assumed): This is `superpowers`, a personal/community Claude Code plugin repository — markdown skill files plus small JS helper scripts. Blast radius of a defect is one developer's agent workflow and tokens: no end users, no user data, no payments/PII, no production deployment, no on-call. Code lifetime is long (skills and helpers are reused across sessions for months) and the audience is the maintainer plus other agents. No network-exposed surface in normal use; resilience/observability/cost findings are calibrated down, but injection/authZ/secrets floors on surfaces processing outside input remain profile-proof.
inputs: branch super-roast-live-test@a71e3a4 vs base 5327d4d (clean tree)
coverage: 8 scouts dispatched (0 dead), dedupe alive · 65 raw → 32 deduped → 12 panel + 12 spot-checked (0 promoted), 8 beyond panel cap · judge completion 69%
independence: same-family (Claude) — seat-differentiated panel

## Confirmed findings            ← consumed by super-plan, one task per finding
- [Blocking] scripts/collect-metrics.js:4 (also 89-92) — The script cannot execute at all: it is CommonJS (`require`, `module.exports`, `__dirname`, `require.main`) but the root package.json declares "type": "module", so Node parses it as ESM and throws `ReferenceError: require is not defined in ES module scope` at line 4 before any logic runs; both `require()` and `import()` of the file fail identically, so no test can load it either.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Reproduced independently by all three seats on node v26.4.0, including a fresh checkout of a71e3a4: `node scripts/collect-metrics.js --project test` exits 1 at line 4 with the exact ReferenceError; no intermediate scripts/package.json exists to override the root's "type": "module". Repo's own convention for CJS is the .cjs extension (skills/brainstorming/scripts/server.cjs). Meets the profile-proof floor: the change's entire stated purpose (a runnable collector) fails 100% at first invocation.
  fix-shape hint: Rename to scripts/collect-metrics.cjs (matching the repo's server.cjs convention) or convert the file to ESM imports/exports.
- [Blocking] scripts/collect-metrics.js:6 — `require('./db-client')` has no resolvable target anywhere in the repo (and package.json declares zero dependencies), so even with the ESM/CJS problem fixed the script dies at module load with MODULE_NOT_FOUND on every invocation; the unconditional top-level require also makes even the pure exported helpers unloadable in tests, and leaves `db.query`'s escaping/binding contract unreviewable.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: `git grep 'db-client' $(git rev-list --all)` matches only this line across all history; reproduced by renaming to .cjs and running — `Error: Cannot find module './db-client'`, code MODULE_NOT_FOUND, at line 6 before parseArgs, outside every try/catch, in every mode. Nothing in the commit discloses db-client as a deferred dependency.
  fix-shape hint: Add the db-client module (with an explicit, documented query contract) or inject the client as a parameter so the script and its helpers can load without it.
- [Should-fix] scripts/collect-metrics.js:16 (flows into 76) — `--interval` is never validated: a missing or non-numeric value yields NaN (zero/negative pass through too), which setInterval coerces to the 1 ms minimum, turning watch mode into a ~800/sec loop issuing one DB query, one recursive mkdir, one file rewrite, and one log line per tick, forever — the catch at 70-75 swallows failures so it never self-terminates, and the only signal is a one-shot TimeoutNaNWarning plus a banner reading "every NaNms".
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: All three seats reproduced the ~1 ms clamp live on node v26.4.0 (236-242 ticks/300 ms across probes); ground seat additionally cited the Node timers doc verbatim ("When delay is larger than 2147483647 or less than 1 or NaN, the delay will be set to 1"). parseArgs (line 16) does `parseInt(argv[++i], 10)` with no NaN/range check; main guards only `!args.project`.
  fix-shape hint: Validate with Number.isFinite(interval) && interval > 0 after parseArgs and exit with the usage error otherwise.
- [Should-fix] scripts/collect-metrics.js:39-41 — The `--project` CLI value is string-concatenated into a single-quoted SQL literal with no escaping, binding, or validation: any project name containing an apostrophe produces malformed/altered SQL (a plain correctness break), and a crafted name closes the literal for UNION/stacked injection if the eventual driver permits it; no test pins any quoting behavior and `fetchProjectMetrics` is not even exported.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: All seats verified lines 39-41 verbatim (`"...WHERE project = '" + project + "'..."` → `db.query(query)`) with nothing between argv parse (line 15) and query construction; `--project "bob's-app"` demonstrably yields invalid SQL. Injection floor checked and not fired: profile states no network-exposed surface, argv originates from the developer/own CI config, and all three seats independently declined Blocking on that basis (exploit ceiling also depends on the absent db-client).
  fix-shape hint: Use parameter binding once db-client exists, or in the interim strictly allow-list the project name (e.g. /^[A-Za-z0-9._-]+$/) before query construction.
- [Should-fix] scripts/collect-metrics.js:23-31, 46-55 — NULL or negative columns in a fetched row are silently rendered as garbage that still ships with status "ok": null/undefined/negative artifact_bytes becomes the literal string "NaN undefined" and null duration_ms becomes "0m 0s" (indistinguishable from a genuinely instant build); summarize guards only the whole row, never individual columns, and no test covers any nullish/non-positive input.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: All three seats executed the committed functions: formatBytes(null|undefined|-5) → "NaN undefined", formatDuration(null) → "0m 0s"; end-to-end summarize of a null-columned row wrote {"status":"ok","duration":"0m 0s","size":"NaN undefined"} as an authoritative dashboard summary. Only guard is `if (!row)` at line 47.
  fix-shape hint: Per-column Number.isFinite/non-negative guards in summarize, emitting a distinct degraded status (or nulls) instead of formatting garbage under "ok".

## Not verified (beyond panel cap)   ← severe candidates the panel cap left unverified — listed, never dropped
- [suggested Should-fix] scripts/collect-metrics.js:47, 62 — A transient empty query result unconditionally overwrites the last-good summary with `{"status":"no-data"}`, destroying the only artifact consumers read, with no keep-last-good guard or write-only-on-change check.
- [suggested Should-fix] scripts/collect-metrics.js:67-77 — The watch daemon has no stop path at all: the setInterval handle is discarded, no SIGINT/SIGTERM handler exists, and no db close/release appears on any path — stops interrupt at arbitrary points (including mid-write, leaving truncated JSON), connections may leak, and watchLoop is structurally untestable.
- [suggested Should-fix] scripts/collect-metrics.js:69 — Watch mode performs no immediate first collection: the output file is not written until one full interval elapses, so a fresh watcher serves ENOENT or stale data for that window, and a crash-looping watcher never writes anything at all.
- [suggested Should-fix] scripts/collect-metrics.js:11-21 — The hand-rolled flag parser silently mis-parses malformed input (a value-taking flag consumes a following flag; trailing `--interval` swallows `--project`; typos are dropped without a word) where stdlib util.parseArgs covers exactly this shape.
- [suggested Should-fix] scripts/collect-metrics.js:46-55, 62 — summarize bakes lossy human display strings ("1m 30s", "1.20 MB") into the persisted machine-readable JSON, dropping the raw numeric duration_ms/artifact_bytes that downstream consumers would need.
- [suggested Should-fix] scripts/collect-metrics.js:89 — The diff ships a 92-line script whose only exported surface exists for testing yet includes zero tests, in a repo with an established tests/ tree and a direct precedent (tests/brainstorm-server) for testing a JS script; nothing would run such tests anyway (no scripts block, no CI workflows).
- [suggested Should-fix] scripts/collect-metrics.js:6, 41 — The database client is a hard module-scope require rather than an injected dependency, so any test of the fetch path must stub the module registry instead of passing a fake — a pattern with no precedent in this repo.
- [suggested Should-fix] scripts/collect-metrics.js:39-41 — The polled query filters on project and sorts created_at DESC LIMIT 1; without a composite (project, created_at) index each poll scans/sorts the project's full history, paid repeatedly every watch tick on an append-only table.

## Rejected (with reason)        ← so re-roasts don't re-litigate
- scripts/collect-metrics.js:9, 55-63 (testing — repo-tree pollution): refute landed — the file is a documented throwaway eval fixture (docs/superpowers/plans/2026-07-29-super-roast.md), docs/testing.md's cleanup convention targets bash-driven skill tests not Node scripts, and no test exists in the repo that could be polluted; the claimed failure describes a hypothetical test, not this diff.
- scripts/collect-metrics.js:86 (stdout contract differs by mode): refute landed — the machine-parseable contract is the written JSON file, identical in both modes; no stated stdout contract exists, and a per-interval JSON blob on a daemon's stdout was never a documented usage.
- scripts/collect-metrics.js:72 (unconditional heartbeat line, ~2,880/day): refute landed — mechanics accurate, but no stated intent or repo convention commits to bounded/configurable log verbosity; a --quiet knob is a feature ask beyond the change's stated scope, not a defect.
- scripts/collect-metrics.js:57-64 (runOnce carries four responsibilities): refute landed — summarize, the pure logic worth isolating, is already exported and disk-free; runOnce is orchestration wiring, and no stated contract or existing test scaffolding demands the decomposition.
- scripts/collect-metrics.js:62, 72 (no metric history retained): refute landed — the stated contract is a live current-state summary for a dashboard ("reads the latest build_metrics row... writes a JSON summary"); overwrite-per-poll is the correct implementation of that, and historical regression reconstruction is an unstated goal the finding itself disclaims.

## Unverified nits (spot-checked)
- [Nit] scripts/collect-metrics.js:23-31 — Formatters mislead at range edges: durations never roll up past minutes ("125m 0s" for 2h5m), negatives mangle ("-1m -2s"), sub-KB sizes print fractional bytes ("500.40 B"); cosmetic only.
- [Nit] scripts/collect-metrics.js:74 — Watch-mode failures log a one-liner with only err.message — no stack, code, timestamp, or project — while the adjacent success line carries ISO timestamp and project.
- [Nit] scripts/collect-metrics.js:47 — The no-data state is reported at the same log level and shape as success and exits 0, so a project that silently stopped emitting rows is indistinguishable from healthy to exit-status/log-severity consumers.
- [Nit] scripts/collect-metrics.js:81-84 — `process.exit(1)` immediately after console.error can discard the usage message when stderr is a pipe (Node does not flush pending async pipe writes on exit); safe form is `process.exitCode = 1` and return.
- [Nit] scripts/collect-metrics.js:60-62 — runOnce recomputes the invariant output path and re-issues recursive mkdirSync every poll and rewrites byte-identical JSON unconditionally; negligible at 30 s, material only combined with the NaN-interval defect.
- [Nit] scripts/collect-metrics.js:89 — Five internals exported for reuse that nothing in the repo consumes — a speculative library surface on a 92-line CLI.
- [Nit] scripts/collect-metrics.js:57, 67 — runOnce/watchLoop take the whole CLI args bag instead of the two values each uses, leaking flag names into the collection functions.

## Escalations (need human)      ← UNVERIFIED externals, incomplete panels, material dissent
- scripts/collect-metrics.js:60-62 (path traversal via --project into mkdirSync/writeFileSync): dead seat — valid 2/3 (2 CONFIRM at Should-fix); note the surviving refute seat found the finding's own runtime probe claim unreproducible (script crashes before main) and likely fabricated, though the traversal itself was independently re-demonstrated — needs a human call.
- scripts/collect-metrics.js:41, 47 (unverified db.query return-shape contract; garbage-as-"ok" / unhandled-rejection daemon death): dead seat — valid 2/3 (2 CONFIRM at Should-fix), and the external premise (db-client's actual contract) is unresolvable because the module does not exist in the repo; spike proposed in the finding.
- scripts/collect-metrics.js:34-42 (write-only metricsCache contradicting its own comment): dead seat — valid 2/3 (2 CONFIRM at Should-fix); needs a third eye or acceptance as-is.
- scripts/collect-metrics.js:62 (non-atomic truncate-then-write publish, no temp-file+rename): no judge seats returned — valid 0/3; entirely unverified.
- scripts/collect-metrics.js:41, 69-76 (fully synchronous poll inside setInterval — hung query stalls the daemon with no timeout/watchdog): no judge seats returned — valid 0/3; entirely unverified.
- scripts/collect-metrics.js:69-77 (permanently-failing daemon stays observably healthy: swallow-all catch, no backoff/exit-code mutation, always exits 0): no judge seats returned — valid 0/3; entirely unverified.
- scripts/collect-metrics.js:46-55, 70-75 (consumers cannot detect staleness: last-good file left in place on failure, no collection timestamp in the summary): no judge seats returned — valid 0/3; entirely unverified.
---